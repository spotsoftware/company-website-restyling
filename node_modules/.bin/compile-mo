#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir//bin/bash" ]; then
  "$basedir//bin/bash"  "$basedir/../i18n-abide/bin/compile-mo.sh" "$@"
  ret=$?
else 
  /bin/bash  "$basedir/../i18n-abide/bin/compile-mo.sh" "$@"
  ret=$?
fi
exit $ret
