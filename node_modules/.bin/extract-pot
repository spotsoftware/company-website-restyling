#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../i18n-abide/bin/extract-pot" "$@"
  ret=$?
else 
  node  "$basedir/../i18n-abide/bin/extract-pot" "$@"
  ret=$?
fi
exit $ret
